name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.x'

      - name: Extract version
        id: version
        shell: pwsh
        run: |
          $v = '${{ github.ref_name }}'.TrimStart('v')
          echo "value=$v" >> $env:GITHUB_OUTPUT

      - name: Publish EXE
        shell: pwsh
        run: |
          dotnet publish Accentra.csproj `
            -c Release `
            -r win-x64 `
            --self-contained true `
            -p:PublishSingleFile=true `
            -p:IncludeNativeLibrariesForSelfExtract=true `
            -p:Version=${{ steps.version.outputs.value }} `
            -o publish

      - name: Install WiX
        run: |
          dotnet tool install --global wix
          wix extension add --global WixToolset.Util.wixext

      - name: Build MSI
        shell: pwsh
        run: |
          wix build Accentra.wxs `
            -arch x64 `
            -d Version=${{ steps.version.outputs.value }} `
            -d SourceDir=publish\ `
            -o publish\Accentra.msi

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: Accentra ${{ github.ref_name }}
          body: "Download `Accentra.msi` and run it — installs itself and starts with Windows."
          files: publish/Accentra.msi
